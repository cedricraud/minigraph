<p id="notice"><%= notice %></p>

<table>
  <tr>
    <td><img src="<%= @page.thumbnail %>" /></td>
    <td><%= @page.title %></td>
    <td>« <%= @page.description %> »</td>
  </tr>
</table>


<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
